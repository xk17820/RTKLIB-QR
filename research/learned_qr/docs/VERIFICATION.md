# 实现与验证记录（2026-09-19）

## 本次交付

顶层 `src/rtkpos.c`、`src/rtklib.h` 已接入 Q/R；新增纯C推理/稳定数值核/原始RINEX会话接口，Python实时条件一阶FLOAT训练、参考CSV、模型导出、回放和评估。`tools/apply_patch.py` 不是运行新版的前置步骤。

## 实际执行的本地验证

1. `cmake --build build -j4`：全项目原生目标构建成功。
2. `ctest --test-dir build --output-on-failure`：**55/55通过**；原始日志 `../validation/ctest-live.txt`。
3. 在本目录运行 `OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 python -m pytest -q`：**47通过，8.12秒**；日志 `../validation/pytest-live.txt`。

新增测试不是只有桩：实际加载重编译的 `librtklib.so`、使用仓库附带 rover/base RINEX 和广播星历，检查两分支梯度、两轮交替优化、权重导出/C回放、C客户端与Python会话一致性、参考时间无交集、非法配置/模型、回调异常、会话重启、协方差对称性和有效性。测试参考标签是人工构造的，**不是独立高精度真值**。

原始整数保护测试对 `resamb_LAMBDA`、`manage_amb_LAMBDA`、`holdamb`、`ddcov` 及 `src/lambda.c` 的受保护文本做SHA256对比。它证明代码未改，不证明整数固定结果必然相同。

## 关闭学习回归与稳定核对照

使用 `test/data/rinex/30400920.05o`、`07590920.05o` 及对应导航文件，GPST 2005-04-02 00:00:00–00:10:00、21个输出历元。参考无关：只比较程序输出数据行，不比较随路径变化的文件头。

- 本地保留的未修改原生二进制与新版默认关闭学习二进制：**所有21行逐字节一致**。
- 稳定核关闭学习与稳定核+全1倍率模型：**所有21行逐字节一致**。
- 记录见 `../validation/baseline-regression.json`。

这只是该片段的回归，不声称所有平台和所有数据逐位一致。学习路径数值核是 Cholesky/Joseph，旧核基线与稳定核基线必须分别报告，不能将数值改造增益混成学习增益。

## 不作出的结论

没有在用户的实际路线训练或计算定位精度；没有真实训练权重随仓库分发；没有声称提高固定率或降低真实错误整数固定率。代码支持用户自行做这些实验。

训练为条件一阶EKF梯度：局部H、旋转、特征提取和离散选择停止梯度；并非全C自动微分。支持的短基线profile和平台边界详见 `TRAINING.md`，不把未支持的估计状态模式默默当作已实现。原论文还包含bias等设计，本分支按用户要求不含这些分支。

远端CI结果以对应提交的 GitHub Actions 为准；本文件中的47/55是上述本地实际测试结果，不提前声明远端成功。
