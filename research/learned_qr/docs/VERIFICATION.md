# 本次源码发布验证

日期：2026-09-17。仅验证研究原型组件，不是完整 RTKLIB 或定位性能验收。

## 实际执行

```text
OMP_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1 python -m pytest -q
..........................                                               [100%]
26 passed in 3.64s
```

环境：Linux CPU、Python 3.13.5、PyTorch 2.10.0+cpu、NumPy 2.3.5、pytest 9.0.2。
原始输出：`../validation/pytest-publish.txt`。

组件测试涵盖 C99 测试桩编译、C/PyTorch 非平凡输出一致性、双差相关项、梯度传播、独立缓存、重复历元、周跳/信号切换/超时重置、默认关闭/模型拒绝，以及补丁对小型接口样例的检查。

发布通过 GitHub Git 数据接口创建源码树和研究分支。18 个原型源码/测试/许可证/配置文件的远端 Git blob SHA 已与本地通过测试的文件逐一核对，内容逐字节一致。文档根据本次发布范围重新整理，不沿用旧文档中的连接状态描述。

## 未验证与未完成

- 本次没有应用补丁到仓库顶层 RTKLIB 源码；`src/rtkpos.c` 和 `src/rtklib.h` 保留上游版本。
- 没有完整 RTKLIB 编译、真实 RINEX 基线回归或真实 GNSS 训练。
- 没有训练好的真实模型权重，也没有定位提升、固定率或错误固定率结论。
- 不将接口测试桩等同于完整 RTKLIB；不将合成训练等同于真实 GNSS 实验。

这次发布的目的是保存可审查、可测试的研究原型，不是宣布整个方法实现完成。
