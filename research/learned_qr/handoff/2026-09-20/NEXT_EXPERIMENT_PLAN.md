# SCL-RTK后续执行协议（已同意，未完成）

此文把此前用户已确认的方案存入仓库，避免新聊天重复规划。工作前读HANDOFF.md和SNAPSHOT_STATUS.json。

## P0：恢复可信开发状态

复现当前identity梯度失败；记录实际torch/编译器；明确阈值点的导数约定，不能先删测试。核对C和PyTorch前向、V1/V2/V3格式及特征维度兼容。新增回归应先失败、修正后通过。原生断言必须打开。源码与研究目录镜像同步，不能编译到旧共享库。

## P1：先验证新增方法，再扩数据

核验候选获取与正常LAMBDA/固定管理解耦：不能为训练改变部署候选或固定接受规则；记录候选的实际模糊度子集、DD变换、整数值、Q_aa/Q_pa、float与conditional位置。参考只进入loss。训练中的候选停止梯度；连续条件关系做局部数值差分检查。明确未触发候选、病态Q_aa、重置、周跳、长间隔和fix-and-hold行为。避免只对成功FIX样本计算损失导致网络通过减少FIX逃避惩罚。

代码应清楚划分：Q运动预测失配、码R、受限相位策略；一致性特征而非绝对C/N0硬门控独占决策。先保持相位原模型作必要基线，再证明新增相位策略的独立收益。Q/R保证正值、幅度有界、保留DD非对角相关；不回归bias或坐标。

## P2：数据计划，计划量不得写入已完成结果

|划分|数量|每条预定历元|总历元|
|---|---:|---:|---:|
|训练|32|1800|57600|
|验证|8|1800|14400|
|完整主测试|12|3600|43200|
|压力测试|12类×3实例|1800|64800|
|合计|88|—|180000|

1Hz约50小时模拟时长，不是运行时间。至少3个预先固定训练初始化seed。需要改变起始时段/几何、短基线长度、运动激励、误差相关性、接收机质量报告偏移，而非只换随机噪声。旧已分析数据归开发/回归，不再称盲测。冻结训练/验证/测试清单、生成器和哈希后再试验。

单项压力：码与相位污染分离；标记/未标记周跳；参考星退出；少星；基站污染；多长度中断与恢复；真实异步与时间标签错误分开；高C/N0污染与正常弱信号。完整长序列场景切换不人工重启；既有长间隔重置规则固定公开。无解槽保留状态0/NaN，覆盖分母使用所有预定槽。

PPC-Dataset/UrbanNav只能在原始观测、星历、真值确实取得后列实测。PPC rover/base采样率不同，参考点/杆臂说明曾被指出不一致，必须核实，不猜参数。跨城市不等于跨接收机。下载失败如实记录，不能用模拟替代而标实测。

## P3：四组主实验

A 随机模型：误差二阶矩/预测方差、创新统计、Q与运动变化、码与相位分别分析；有偏误差不轻率当作零均值方差标定。

B 完整轨迹：经验RTK、相关性保留的鲁棒RTK、验证集选定的固定规则RTK、SCL-RTK四种主比较。旧自由倍率/诊断候选/身份回退/数值核差异放补充材料，关键反例仍需正文。

C 独立重训消融：去Q、去一致性、去条件固定位置loss、去结构约束；明确计算/优化预算。仅切换同一权重输出开关不是独立重训消融。

D 泛化与压力：新几何、误差组合、特征偏移、周跳/中断/少星恢复；记录延迟、数值失败与训练成本。完整路线和单项诊断分开，不混成一张不解释的排名表。

## P4：指标与模型选择

报告H/U/3D RMS、P95/P99、最大误差、覆盖率、固定率，以及FIX位置超限/全部槽和超限/接受FIX两种分母、连续事件数/持续时间。模拟中尽可能比对真实接受的整数向量；没有该核验时保留“位置超限代理”名称。配对路线为统计单位，不把高相关的历元当独立样本。报告所有初始化seed与接受率，不挑最好一次。

训练目标包括FLOAT位置、条件固定位置、统计一致性和偏离基线正则；阈值/权重只从开发/验证确定。冻结正式模型后才跑新测试。所有候选失败时报告拒绝，不能用末轮替补并称主方法成功。

## P5：论文结构和写作顺序

I引言（问题、相关研究、与伪距DGNSS区别、实质贡献）。II方法（DD码/相位与动态模型、SCL框架、Q/R结构与一致性、参考/条件固定位置目标与训练）。III数据与结果（随机模型分析→完整轨迹→重训消融→场景及泛化→成本）。IV结论。源码哈希、软件测试、拒绝候选和旧版本归附录；重要失败边界不删除。

先完成可核查的表和图，再写摘要与结论。原稿参考只是组织写法，不复制作者、经费、实测设备或实验数字。不得用新名称包装旧模型成绩。
